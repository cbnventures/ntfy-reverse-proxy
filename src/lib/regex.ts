/**
 * Country code array.
 *
 * @since 1.0.0
 */
export const countryCodeArray = /^[A-Z]{2}$/;

/**
 * Https url.
 *
 * @since 1.0.0
 */
export const httpsUrl = /^https:\/\//;

/**
 * Ntfy topic array.
 *
 * @since 1.0.0
 */
export const ntfyTopicArray = /^([a-z-0-9]+) \|\| ([a-z0-9]+)$/;

/**
 * User agent format.
 *
 * @since 1.0.0
 */
export const userAgentFormat = /^([a-z-]+)(\/)([0-9.]+)$/;
